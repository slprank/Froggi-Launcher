const COL = 512;
const ROW = Math.floor((COL * 9) / 16);
const MIN = Math.floor(COL / 100);

const FRONTEND_PORT = 3200;
const WEBSOCKET_PORT = 3100;

export { COL, ROW, MIN, FRONTEND_PORT, WEBSOCKET_PORT };
